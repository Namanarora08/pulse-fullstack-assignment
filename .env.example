DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pulse?schema=public"

# HMAC key used to sign session cookies. Required in production (>= 16 chars).
# Generate with: openssl rand -base64 32
SESSION_SECRET=""

# Demo login credentials (override in any shared/deployed environment).
DEMO_DOCTOR_PASSWORD=""
DEMO_ADMIN_PASSWORD=""
